@model System.Data.DataTable

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

@*<p>
        @Html.ActionLink("Create New", "Create")
    </p>*@
<table class="table table-striped">
    <tr>
        <th>
            Computed Cost Id
        </th>
        <th>
            Priority
        </th>
        <th>
            Type
        </th>
        <th>
            Weight
        </th>
        <th>
            Cost
        </th>
        <th>
            Total Cost
        </th>
        <th></th>
    </tr>

    @for (int i = 0; i < Model.Rows.Count; i++)
    {
        <tr>
            <td>
                @Model.Rows[i][0]
            </td>
            <td>
                @Model.Rows[i][1]
            </td>
            <td>
                @Model.Rows[i][2]
            </td>
            <td>
                @Model.Rows[i][3]
            </td>
            <td>
                @Model.Rows[i][4]
            </td>
            <td>
                @Model.Rows[i][5]
            </td>
            <td>
                <a href="@Url.Action("Edit", "ComputedCost", new { @id = @Model.Rows[i][0] })" >Edit</a> |
                <a href="@Url.Action("Details", "ComputedCost", new { @id = @Model.Rows[i][0] })">Details</a>
@*@Html.ActionLink("Details", "Details", new { id = item.computedcostid }) |*@
    @*@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
</td>
</tr>
    }

</table>
